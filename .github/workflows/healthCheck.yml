name: Health check

on:
  schedule:
    - cron:  '37 * * * *'

jobs:
  health_check_job:
    runs-on: ubuntu-20.04
    steps:
    - name: Check the deployed service URL
      uses: jtalk/url-health-check-action@v3
      with:
        url: https://ak0411-pokedex.fly.dev/